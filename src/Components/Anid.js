import React from "react";


// example of Destructing props and state
const Anid=({name,heroName})=>{

    

    return(

        <div>
            <h1>Hello{name} is {heroName}</h1>
        </div>
    )
}

export default Anid